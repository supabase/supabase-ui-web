---
title: Input
description: Basic button component
img: "/images/button.svg"
---

## Installing

Install the component

```cli
npm install @supabase/ui
```

## Getting started

Import the component

```js
import { Input } from '@supabase/ui'
```

## Basic

An Input component will default to using `type="text"`. You can define a `label`.

`type` can be used to show what kind of input you need. It supports all Input types, except for the following:

- Number, use [InputNumber](/components/inputnumber)
- Radio, use [Radio](/components/radio)
- Checkbox, use [Checkbox](/components/checkbox)

<CodeSample sample="InputBasic" v="0" />

## Input labels

You can use `label`, `descriptionText` and `labelOptional` to illustrate what your Input is for and demonstrate to the user what they need to input.

<CodeSample sample="InputLabels" v="0" />

## Icon

use `Icon` to inject any Icon component you like.

<CodeSample sample="InputIcon" v="0" />

## Copy value

Use `copy` to add a copy button. It will copy the value of `value`

<CodeSample sample="InputCopy" v="0" />

## Reveal and copy

Use `copy` to add a copy button. It will copy the value of `value`

<CodeSample sample="InputReveal" v="0" />

## Actions

Use `actions` to inject any React.ReactNode jsx

<CodeSample sample="InputControls" v="0" />

## Error

Use `error` with a string to show an error message

<CodeSample sample="InputError" v="0" />

## Text Area

Use `<Input.TextArea>` to use a text area input

<CodeSample sample="InputTextArea" v="0" />

## Text Area with limit

Use `<Input.TextArea>` to use a text area input

<CodeSample sample="InputTextAreaWithLimit" v="0" />

## API Reference

#### Input

<ComponentProps signature={`"Input/Input"`} />

#### Input.TextArea

Coming soon
