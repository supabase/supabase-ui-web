import { IconMail } from '@supabase/ui'

export default function IconSizes() {
  return (
    <>
      <IconMail strokeWidth={1} />
      <IconMail strokeWidth={2} />
      <IconMail strokeWidth={4} />
    </>
  )
}
