---
title: Icon
description: Icon component
img: "/images/icon.svg"
---

## Installing

Install the component

```cli
npm install @supabase/ui
```

## Getting started

Import the component

```js
import { IconMail } from '@supabase/ui'
```

## Basic

We use Feather icon library in Supabase UI

You can use any Icon from the library as a component by prepending Icon to any Icon name, like, <IconMail />

<CodeSample sample="IconBasic" v="0" />

## Icon Size 

You can define the size of an icon using the `size` prop.
The `size` prop is a `font-size` points (pt) integer 

<CodeSample sample="IconSize" v="0" />

## Icon Stroke Width 

You can define the stroke width (line thickness) using the `strokeWidth` prop

<CodeSample sample="IconStroke" v="0" />

## API Reference

