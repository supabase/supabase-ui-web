---
title: Card
description: Basic button compoment
img: "/images/button.svg"
---

## Installing

Install the component

```cli
npm install @supabase/ui
```

## Getting started

Import the component

```js
@import { Card } from '@supabase/ui'
```

## Basic

You can define the type of a button using the `type` prop. There are `primary`, `default`, `secondary`, `outline`, `dashed`, and `text`,

<CodeSample sample="CardBasic" v="0" />

## Cover

You can define the type of a button using the `type` prop. There are `primary`, `default`, `secondary`, `outline`, `dashed`, and `text`,

<CodeSample sample="CardCover" v="0" />

## Meta

You can define the type of a button using the `type` prop. There are `primary`, `default`, `secondary`, `outline`, `dashed`, and `text`,

<CodeSample sample="CardMeta" v="0" />

## Hoverable

You can define the type of a button using the `type` prop. There are `primary`, `default`, `secondary`, `outline`, `dashed`, and `text`,

<CodeSample sample="CardHoverable" v="0" />
