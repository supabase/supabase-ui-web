---
title: Select
description: Basic button compoment
img: "/images/button.svg"
---

## Installing

Install the component

```cli
npm install @supabase/ui
```

## Getting started

Import the component

```js
@import { Input } from '@supabase/ui'
```

## Basic

An Input component will default to using `type="text"`. You can define a `label`.

<CodeSample sample="SelectBasic" v="0" />

## Controlled

An Input component will default to using `type="text"`. You can define a `label`.

<CodeSample sample="SelectControlled" v="0" />

## Errror

An Input component will default to using `type="text"`. You can define a `label`.

<CodeSample sample="SelectError" v="0" />

## Icon

An Input component will default to using `type="text"`. You can define a `label`.

<CodeSample sample="SelectIcon" v="0" />

## Option group

An Input component will default to using `type="text"`. You can define a `label`.

<CodeSample sample="SelectOptionGroup" v="0" />

## API reference

| Syntax    | Description |
| --------- | ----------- |
| Header    | Title       |
| Paragraph | Text        |
